## Workshop LLM Prompts

# Analytics
- What datasets are available in the dremio catalog?
- Which datasets should be used in production?
- Preview 10 rows from the workshop.gold.trips_enriched view as a table
- Is rain or snow more common in the gold layer dataset?
- Is there a correlation between rain and the price of a taxi ride?
- Plot an ascii bar graph of passenger count against total fare amount

# Data Transformation
- List the bronze layer datasets
- Show me the raw SF weather dataset
- Are there any columns in the raw SF weather data that are missing values? Show as a table with missing values as a count and as a percentage of all rows?
- Write an SQL select statement to drop the empty column and rename the remaining columns to human readable names
- Write an SQL select statement to drop the empty column and rename the remaining columns to human readable names. Turn the attributes into appropriate data types. Have it handle empty strings. Save this view as sf_weather in the silver layer of the catalog
- How many records in the  NYC weather silver dataset occur in the period covered by the SF weather silver dataset
- Show me 10 of these records
